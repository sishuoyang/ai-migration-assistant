# Prompt 05 — Query Rewriting

> Rewrite the queries in `sources/snowflake/queries/sample_olap_queries.sql`
> for ClickHouse. Highlight every Snowflake-specific function or syntax that
> changed and explain why — consult the `clickhouse-docs` MCP for the right
> ClickHouse function names if you're not sure.
>
> Execute each rewritten query against `migration_target` via `clickhousectl`
> to verify correctness on the migrated data.
