# Prompt 02 — Query Pattern Analysis

> Open `sources/snowflake/queries/sample_olap_queries.sql`, paste the contents
> into the chat, and ask the agent to derive ORDER BY key and partition
> recommendations from the columns that appear in WHERE / JOIN / GROUP BY.
> Look up any unfamiliar Snowflake functions in the docs as you go.
