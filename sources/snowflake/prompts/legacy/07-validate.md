# Prompt 07 — Validate and Generate the Post-Migration Report

> Cross-check the migrated schema and data against
> `sources/snowflake/queries/expected_ch_schema.sql` and
> `expected_ch_queries.sql`. Generate the post-migration HTML report
> covering: row count comparison per table, type coercions applied,
> Snowflake-specific objects handled vs deferred (with rationale), any
> outstanding follow-ups (Time Travel, Tasks, etc.).
