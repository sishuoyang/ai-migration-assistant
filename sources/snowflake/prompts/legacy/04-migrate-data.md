# Prompt 04 — Data Migration

> Present both migration paths (Python via `migration-runner` vs direct
> `s3()` via `clickhousectl`), let me pick the one I want, then execute.
> Validate row counts on every migrated table at the end and flag any
> discrepancies.
