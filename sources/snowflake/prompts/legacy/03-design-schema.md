# Prompt 03 — ClickHouse Schema Design

> Design the ClickHouse target schema. Use the `clickhouse-docs` MCP to
> consult best practices for ORDER BY, partitioning, and type selection.
> For every Snowflake-specific object discovered in prompt 01, decide
> whether to recreate it, replace it with a different mechanism, or skip
> it — explain the reasoning.
>
> Wait for my explicit confirmation before executing any DDL via the
> `clickhousectl` MCP.
